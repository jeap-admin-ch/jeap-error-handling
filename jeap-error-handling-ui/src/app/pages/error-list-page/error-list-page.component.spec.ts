import {ErrorListPageComponent} from './error-list-page.component';

describe('ErrorListPageComponent', () => {
	let component: ErrorListPageComponent;

	beforeEach(() => {
		component = new ErrorListPageComponent();
	});

	it('should create', () => {
		expect(component).toBeTruthy();
	});
});
